########## Main Hyprland config (modular) ##########

# Order matters: monitors → input → autostart → appearance → keybinds → rules

source = monitors.conf
source = input.conf
source = autostart.conf
source = appearance.conf
source = keybinds.conf
source = rules.conf
