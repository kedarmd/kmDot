# Nord
# Built-in theme "nord". sidebar_bg is pinned because built-in themes
# leave it transparent (shows the host terminal's background).
[theme]
name = "nord"

[theme.custom]
sidebar_bg = "#2e3440"
