# One Dark
# Built-in theme "one-dark". sidebar_bg is pinned because built-in themes
# leave it transparent (shows the host terminal's background).
[theme]
name = "one-dark"

[theme.custom]
sidebar_bg = "#282c34"
