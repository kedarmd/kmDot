# TokyoNight Night
# Built-in theme "tokyo-night". sidebar_bg is pinned because built-in themes
# leave it transparent (shows the host terminal's background).
[theme]
name = "tokyo-night"

[theme.custom]
sidebar_bg = "#1a1b26"
