# Catppuccin Mocha
# Built-in theme "catppuccin" (= Mocha). sidebar_bg is pinned because built-in
# themes leave it transparent (shows the host terminal's background).
[theme]
name = "catppuccin"

[theme.custom]
sidebar_bg = "#1e1e2e"
